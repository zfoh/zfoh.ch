resolver: lts-16.9

extra-package-dbs: []
packages:
- '.'

flags:
  # This significantly speeds up CI builds, and we don't really need Pandoc for
  # this website.
  hakyll:
    usePandoc: false

extra-deps:
- 'hakyll-4.13.4.1'
